interface:
  display_name: "Yorumi"
  short_description: "Repo-specific patterns and workflows for Yorumi"
  default_prompt: "Use the Yorumi repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true